CREATE TABLE IF NOT EXISTS YumRepositories
(
    hostID                          BIGINT UNSIGNED     NOT NULL,
    slug                            VARCHAR(128)        NOT NULL COMMENT 'Must be a unique name for each repository, one word.',
    name                            VARCHAR(128)        NOT NULL COMMENT 'A human readable string describing the repository.',
    failoverMethod                  VARCHAR(10)         NULL COMMENT 'Either "roundrobin" or "priority". "roundrobin" randomly selects a URL out of the list of URLs to start with and proceeds through each of them as it encounters a failure contacting the host. "priority" starts from the first baseurl listed and reads through them sequentially.',
    baseURI                         VARCHAR(256)        NULL COMMENT 'Must be a URL to the directory where the yum repository''s ''repodata'' directory lives. Can be an http://, ftp:// or file:// URL.',
    repoType                        VARCHAR(16)         NULL,
    metadataExpiration              VARCHAR(8)          NULL COMMENT 'Time (in seconds) after which the metadata will expire. So that if the current metadata downloaded is less than this many seconds old then yum will not update the metadata against the repository. If you find that yum is not downloading information on updates as often as you would like lower the value of this option. You can also change from the default of using seconds to using days, hours or minutes by appending a d, h or m respectively. The default is 6 hours, to compliment yum-updatesd running once an hour. It''s also possible to use the word "never", meaning that the metadata will never expire. Note that when using a metalink file the metalink must always be newer than the metadata for the repository, due to the validation, so this timeout also applies to the metalink file.',
    metalinkURI                     VARCHAR(256)        NULL COMMENT 'Specifies a URL to a metalink file for the repomd.xml, a list of mirrors for the entire repository are generated by converting the mirrors for the repomd.xml file to a baseurl. The metalink file also contains the latest timestamp from the data in the repomd.xml, the length of the repomd.xml and checksum data. This data is checked against any downloaded repomd.xml file and all of the information from the metalink file must match. This can be used instead of or with the baseurl option. Substitution variables, described below, can be used with this option. This option disables the mirrorlist option. As a special hack is the mirrorlist URL contains the word "metalink" then the value of mirrorlist is copied to metalink (if metalink is not set).',
    mirrorlistURI                   VARCHAR(256)        NULL COMMENT 'Specifies a URL to a file containing a list of baseurls. This can be used instead of or with the baseurl option. Substitution variables, described below, can be used with this option. As a special hack is the mirrorlist URL contains the word "metalink" then the value of mirrorlist is copied to metalink (if metalink is not set).',
    enabled                         BOOLEAN             NULL COMMENT 'This tells yum whether or not use this repository.',
    gpgKeyURI                       VARCHAR(256)        NULL COMMENT 'A URL pointing to the ASCII-armored GPG key file for the repository. This option is used if yum needs a public key to verify a package and the required key hasn''t been imported into the RPM database.',
    checkGPGSignatureOnPackages     BOOLEAN             NULL COMMENT 'This tells yum whether or not it should perform a GPG signature check on packages.',
    checkGPGSignatureOnRepoMetadata BOOLEAN             NULL COMMENT 'This tells yum whether or not it should perform a GPG signature check on the repodata.',
    gpgCAKeyURI                     VARCHAR(256)        NULL COMMENT 'A URL pointing to the ASCII-armored CA key file for the repository. This is a normal gpg public key - but this key will be used to validate detached signatures of all other keys. The idea is you are asked to confirm import for this key. After that any other gpg key needed for package or repository verification, if it has a detached signature which matches this key will be automatically imported without user confirmation.',
    excludePackages                 MEDIUMTEXT          NULL COMMENT 'List of packages to exclude from updates or installs. This should be a space separated list. Shell globs using wildcards (eg. * and ?) are allowed.',
    includePackages                 MEDIUMTEXT          NULL COMMENT 'Inverse of exclude. This is a list of packages you want to use from a repository. If this option lists only one package then that is all yum will ever see from the repository. Defaults to an empty list. Substitution variables, described below, are honored here.',
    enablePackageGroups             BOOLEAN             NULL COMMENT 'Determines whether yum will allow the use of package groups for this repository.',
    keepAlive                       BOOLEAN             NULL COMMENT 'Set whether HTTP keepalive should be used for HTTP/1.1 servers that support it. This can improve transfer speeds by using one connection when downloading multiple files from a repository.',
    timeoutInSeconds                INTEGER UNSIGNED    NULL COMMENT 'Number of seconds to wait for a connection before timing out.',
    httpCachingEnabled              BOOLEAN             NULL COMMENT 'Determines how upstream HTTP caches are instructed to handle any HTTP downloads that Yum does. This option can take the following values: "all" means that all HTTP downloads should be cached. "packages" means that only RPM package downloads should be cached (but not repository metadata downloads). "none" means that no HTTP downloads should be cached.',
    retries                         TINYINT UNSIGNED    NULL COMMENT 'Set the number of times any attempt to retrieve a file should retry before returning an error. Setting this to 0 makes yum try forever.',
    throttlingBandwidth             VARCHAR(8)          NULL,
    totalBandwidth                  VARCHAR(8)          NULL,
    tlsCACertificateURI             VARCHAR(256)        NULL COMMENT 'Path to the directory containing the databases of the certificate authorities yum should use to verify SSL certificates. Defaults to none - uses system default.',
    tlsClientCertificateURI         VARCHAR(256)        NULL COMMENT 'Path to the SSL client certificate yum should use to connect to repos/remote sites.',
    tlsClientKeyURI                 VARCHAR(256)        NULL COMMENT 'Path to the SSL client key yum should use to connect to repos/remote sites.',
    tlsVerificationEnabled          BOOLEAN             NULL COMMENT 'Whether Yum should yum verify SSL certificates/hosts at all.',
    checkTLSCertificatePermissions  BOOLEAN             NULL COMMENT 'Whether yum should check the permissions on the paths for the certificates on the repository (both remote and local).',
    mirrorlistExpiration            VARCHAR(8)          NULL COMMENT 'Time (in seconds) after which the mirrorlist locally cached will expire. If the current mirrorlist is less than this many seconds old then yum will not download another copy of the mirrorlist, it has the same extra format as metadata_expire. If you find that yum is not downloading the mirrorlists as often as you would like lower the value of this option.',
    proxyURI                        VARCHAR(256)        NULL COMMENT 'URI to the proxy server for this repository.',
    proxyUsername                   VARCHAR(32)         NULL COMMENT 'The username for the proxy, if applicable.',
    proxyPassword                   VARCHAR(32)         NULL COMMENT 'The password for the proxy, if applicable.',
    username                        VARCHAR(32)         NULL COMMENT 'The username to use for basic authentication to a repo or really any URI.',
    password                        VARCHAR(32)         NULL COMMENT 'The password to use with the username for basic authentication.',
    cost                            INTEGER UNSIGNED    NULL COMMENT 'relative cost of accessing this repository. Useful for weighing one repo''s packages as greater/less than any other.',
    skipIfUnavailable               BOOLEAN             NULL COMMENT 'If set to True yum will continue running if this repository cannot be contacted for any reason.',
    UNIQUE INDEX (hostID, slug)
);